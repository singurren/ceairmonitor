# TODO

更新时间：`2026-04-16`

## 当前优先级

1. 把项目整理成更适合 `uv` 管理和迁移的结构
2. 把 Playwright 浏览器探针接成“两级监控”的二级确认器
3. 把“多规则 + 时间窗口 + 航班粒度事件”模型从骨架推进到真实航班确认链路

## 具体待办

- 验证 `uv run ceair-save-state` 在本机环境里能稳定生成可复用的浏览器会话
- 把 `POST /api/browser-probe` 从调试接口升级成可复用的服务内部能力
- 验证新的规则模型在真实场景配置中是否稳定，覆盖：
  - `上海 -> 深圳`，周一 `07:30 - 09:30`
  - `深圳 -> 上海`，周四 `16:00` 以后
  - `深圳 -> 上海`，周五 `12:00` 以后
- 实测常规轮询里的 HTTP -> Playwright 二级确认链路，确认 WAF 场景下仍能抓到 `shoppingv2`
- 评估本机 WSL 与未来 VPS 上 Playwright 会话稳定性的差异
